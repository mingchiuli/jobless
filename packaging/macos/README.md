# Running on macOS

Extract the archive and open `Jobless.app`.

The application is currently unsigned. On a development machine, use
Control-click > Open the first time. Production signing and notarization are
handled in a later release phase.
